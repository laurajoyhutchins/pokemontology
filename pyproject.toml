[project]
name = "pokemontology"
version = "0.1.0"
description = "Replay-backed Pokémon battle mechanics ontology with SHACL validation."
requires-python = ">=3.11"
dependencies = [
  "pytest>=8.0",
  "rdflib>=7.0",
  "pyshacl>=0.30",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
