Scratch queries and ad hoc Turtle fixtures belong here.

Keep codebase-consumed queries in `queries/bundled/`.
The build, docs metadata, and regression suite only read tracked files from `queries/bundled/`.
